{
  "name": "date-based-version",
  "private": true,
  "version": "1.1.0",
  "description": "Tool to generate date-based versions",
  "repository": {
    "type": "git",
    "url": "https://github.com/VincentBailly/date-based-version"
  },
  "main": "./lib/index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest ./src/__tests__",
    "prettify": "prettier --write src",
    "build-dist": "pnpm run build && pnpm run test && node ./buildTools/create-dist.js"
  },
  "author": "Vincent Bailly",
  "bin": {
    "date-base-version": "./bin/setVersion.js"
  },
  "license": "ISC",
  "dependencies": {
    "execa": "^4.0.1"
  },
  "devDependencies": {
    "@types/jest": "^25.2.3",
    "@types/node": "^12.0.0",
    "jest": "^26.0.1",
    "prettier": "^2.0.5",
    "tempy": "^0.5.0",
    "ts-jest": "^26.0.0",
    "typescript": "^3.9.2"
  }
}
